# SSH keys
tes3mp
tes3mp.pub
html
sys.json
test-server/

# Large files — stored in GitHub Releases, not in repo
*.tar
*.zip

# Experimental server (not part of the instructions)
example-server/

# Local backup of GitHub Release assets (russifier, voices, etc.)
releases/
